import classes from "./Header.module.css";

export const Header = () => {
    return (
        <div className={classes.header}>
            <div>some text</div>
            <div>CSV Аналитик CSV Генератор История</div>
        </div>
    );
};
